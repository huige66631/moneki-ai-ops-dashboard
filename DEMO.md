# 第三关演示脚本

## 启动

按 README 的三步启动后，打开 `http://127.0.0.1:5173/`。默认使用 Mock provider，不需要 API key。

## 1 到 3 分钟演示

1. 在“问问经营数据”中点击或输入“牛肉poke 六月卖了多少钱？”，提交后展开“查看查询依据”。指出回答中的金额、订单数、商品身份和日期来自后端白名单查询，不是模型自由填写。
2. 在同一输入框发送“那五月呢？”。说明请求复用了响应中的 `session_id`，服务端只继承已验证的商品参数并替换月份；没有上下文时同一句话会要求补充对象。
3. 点击回答下方的“查看此范围”。看板会通过同一个 dashboard API 切换到回答的真实日期范围，顶部日期、指标、趋势和 Top 10 表一起更新；页面显示一次性确认文案。
4. 在后端目录运行测试，展示会话淘汰和独立数字一致性矩阵：

   ```powershell
   cd backend
   pytest -q
   cd ../frontend
   npm run build
   ```

预期结果：后端全部测试通过，前端生产构建成功。会话是本地内存状态，重启 API 后需要重新发起首问。
