# AI 使用记录

## 用了什么 AI 工具

- Codex（本地 coding agent）
- `impeccable` Skill
- `style-editing` Skill

## 我如何用 Codex 拆分任务

我先把需求拆成四个能独立验收的阶段，并让 Codex 每次只处理一个阶段：

1. **数据层**：先阅读实施计划和三个 CSV 的表头、样本与脏数据，再实现清洗、去重、外键回退、SQLite 入库和审计测试。验收标准是导入可重复执行，指标口径有 fixture 证明。
2. **看板层**：在数据层稳定后，实现 FastAPI dashboard/health 接口，再接 React 日期筛选、指标、趋势和 Top 10。验收标准是页面所有数字来自同一份 API 响应，并覆盖加载、错误和空范围。
3. **问答层**：让 Codex 先写 API 契约，再实现白名单工具、Mock/DeepSeek provider 和确定性回答。验收标准是模型不能写 SQL 或填写数字，金额和订单数必须来自 SQLite。
4. **可信追问层**：最后单独实现会话存储、中文月份解析、独立数值一致性矩阵，以及“查看此范围”的看板联动。验收标准是“六月”到“五月”的追问只继承已验证商品并替换日期。

每个阶段结束后，我都让 Codex 运行对应测试，再根据失败输出继续修复；这样可以把问题定位在数据、API、前端状态或问答解析中的一个边界内。

## 真实 prompt 示例

拆分数据层时，我给 Codex 的任务类似：

> 先阅读 `docs/design/phase-1-dashboard/IMPLEMENTATION_PLAN.md`，检查 `data/raw/` 的表头和异常样本。只实现数据导入和审计，不要接入前端或模型。完成后运行 `backend` 测试，并报告修改文件、测试结果和未覆盖的规则。

拆分可信追问时，我明确限定了边界：

> 实现“牛肉poke 六月卖了多少钱？”后继续问“那五月呢？”。服务端只能继承上一轮成功工具调用中的商品参数，替换日期范围；没有有效会话时返回 `needs_clarification`。先补测试，再改实现，不能从模型回答文本中解析金额或日期。

## Codex 发现并修复了什么

- `Decimal` 进入证据模型后会被序列化成字符串；Codex 在编排器边界转成 JSON number，修复 API 契约漂移。
- 中文月份“六月”不匹配数字正则；Codex 增加中文月份解析，并补上六月到五月的边界测试。
- 前端点击示例后立即提交会读到旧 React state；Codex 让提交函数接收显式问题值。
- 看板日期变化会清空问答证据，导致追问上下文丢失；Codex 改为保留原回答，只有用户点击“查看此范围”才导航。
- 早期接口文档、`.env.example` 和本地默认端口不一致；Codex 统一为后端 `8000`，并把 provider/tool 错误固定为 HTTP 503 的安全响应。

这些修复都通过后端 `pytest -q` 和前端 `npm run build` 验证。

## 哪些部分由我决定并手写

我决定采用 SQLite、FastAPI、React/ECharts 和白名单查询，而不是让模型生成 SQL；我也手写了数据口径、会话继承规则、错误契约和验收问题。原因是这些内容属于产品边界和可追溯性，不能交给模型自行猜测。Codex 负责把这些决定落成代码、测试和文档。

## 密钥与真实 provider

本次实现和测试使用 Mock provider 与本地 SQLite fixture，没有读取或上传外部 API key，也没有发起 DeepSeek 网络请求。填写 `backend/.env` 后才会手动切换真实 provider；密钥始终只由服务端读取。
