# AI 使用记录

## 本次实现使用的工具

- 本地编码代理用于阅读实施计划、现有第一关代码和 API 契约，并实现后端查询工具、Mock/DeepSeek provider、编排器和 React 对话框。
- Impeccable 技能用于遵循既有看板的“经营台账”视觉系统、响应式控件和状态设计。

## 提示词与边界

DeepSeek 的服务端系统提示词位于 `backend/app/ai/prompts.py`。它只允许选择三个白名单工具，不允许生成 SQL 或自行填写数字。最终回答由后端根据工具结果确定性渲染。

## 发现并修复的问题

- 初始实现把 `Decimal` 放进证据模型时会被 Pydantic 序列化为字符串；已在编排器边界转换为 JSON number，保持 API 契约一致。
- 中文月份“六月”不能直接由数字正则解析；已增加中文月份解析并覆盖 Mock 商品提取。
- 示例问题点击后 React 状态尚未更新就提交会读到旧问题；已让提交函数接收显式问题值。

## 未使用真实密钥

本次实现和测试使用 Mock provider 与本地 SQLite fixture，没有读取或上传外部 API key，也没有发起 DeepSeek 网络请求。填写 `backend/.env` 后即可手动切换真实 provider。

## 第三关协作记录

- 根据第三关实施计划实现了 30 分钟过期、100 会话容量的进程内会话存储，以及只允许“继承对象并替换日期”的追问解析。
- 增加独立 SQL/窗口计算的数值一致性矩阵，验证证据、确定性回答和实际数据库口径一致，并覆盖负金额与空数据。
- 前端保留已核对回答，新增“查看此范围”显式动作；日期导航由 App 统一请求 dashboard，不从回答文本解析日期。
- 实际发现并修复的问题：原 AssistantPanel 会在看板日期变化时清除回答，导致追问上下文和证据消失；现在回答保留原始查询范围，只有用户点击后才切换看板。
